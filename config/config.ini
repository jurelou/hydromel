[DEFAULT]
source_index = index=*
summary_index = sigma-hunting
spl_summary = stats values(*) AS * by _time

crontab_schedule = */10 * * * *
allow_skew = 50%%
dispatch_earliest_time = -10m
splunk_app_name = TA-chouchen

[chouchen]
crontab_schedule = 0 * * * *
dispatch_earliest_time = -1h

source_index = index=windows_evtx
summary_index = chouchen-sigma-hunting
